#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
PYTHON="$SCRIPT_DIR/.venv/bin/python"
PYTHONPATH="$SCRIPT_DIR/src" exec "$PYTHON" "$SCRIPT_DIR/src/ctxgen/main.py" "$@"